# Plan 003.3c — Available Tag Vocabulary

**Status:** Planned
**Parent plan:** [Plan 003.3 — Bounded Tag and File Search](003.3-bounded-tag-and-file-search.md)
**Depends on:** [Plan 003.2 — Content-Level Tags](003.2-content-level-tags.md)
**Sequence:** 003.3c
**Created:** 2026-08-24

## Goal

Add an end-to-end, bounded catalog-wide view of the active tag vocabulary: a typed usage query and the new `catalog tags available` CLI command.

## Vocabulary semantics

- Include a canonical tag name only when it has at least one active assertion anywhere in the catalog.
- Tagged content remains represented when it has no current path. Vocabulary membership and counts do not depend on current scans.
- Exclude names whose assertions are all retracted.
- A provenance filter controls both inclusion and every reported count. Without a filter, counts include all active user and system assertions.
- Distinct-content count counts a content identity once per tag even when several active provenance sources assert that tag.
- Assertion, user, and system counts count active assertion rows.
- Optional regex filtering uses Python `re.search` over the complete canonical tag name. Matching is case-sensitive unless the expression supplies an inline flag such as `(?i)`.
- Compile and validate the expression before issuing any catalog query. Invalid expressions fail clearly and do not inspect or mutate catalog state.

## Public API

Add explicit immutable result types with the semantic equivalent of:

- A tag-usage row containing tag name, distinct actively tagged content count, active assertion count, active user assertion count, and active system assertion count.
- A vocabulary search result containing bounded usage rows and the complete number of matching active tag names.

Add a new catalog-wide `Catalog` query with the semantic equivalent of:

```python
search_available_tags(
    *,
    name_regex=None,
    provenance=None,
    sort_by="name",
    reverse=False,
    limit=20,
)
```

`limit` must be positive. Supported sorts are `name`, `content`, and `assertions`. Name defaults ascending; content and assertions default descending. Reverse inverts only the primary direction; canonical tag name remains the ascending deterministic tie-breaker.

The API is catalog-wide and therefore takes no location/root argument. The CLI `ROOT` identifies the in-root catalog database, not a vocabulary scope.

## Query shape and regex integration

- Aggregate only active assertion rows, applying provenance before inclusion and counts.
- Compute the complete matching-tag total before applying the result limit.
- Apply ordering and limit in SQLite before usage rows are materialized.
- Implement Python regex semantics through a connection-local SQLite function bound to the already compiled expression, or an equivalently bounded approach that still applies the result limit in SQLite.
- Do not persist helper state, add tables or indexes, or change schema version 2.

## CLI

Add:

```text
archiver catalog tags available ROOT
    [--regex REGEX]
    [--provenance user|system]
    [--limit N]
    [--sort name|content|assertions]
    [--reverse]
```

- Default to 20 rows sorted by name ascending.
- Render `Tag`, `Content`, `Assertions`, `User`, and `System` columns with deterministic alignment.
- Report the complete matching-tag count and whether all or only the first bounded rows are shown.
- When present, report the regex and effective sort direction.
- Reject invalid regex as a clear CLI usage error before opening/querying the catalog where practical.
- Use the shared standard-library display conventions without introducing color or a third-party table dependency.

## Validation

Add automated coverage for:

- active vocabulary membership, retracted-only exclusion, and content without current paths;
- distinct-content counts versus assertion counts with multiple provenance sources;
- user/system inclusion and counts under provenance filtering;
- Python `re.search` matching, substring matching, inline case-insensitivity, no-match results, and invalid expressions;
- invalid regex failing before SQL execution;
- name, content, and assertion sorts; reverse primary ordering; deterministic alphabetical ties;
- positive limits, complete totals, and SQLite-side row limiting;
- deterministic CLI tables, empty results, fallback terminal width, and invalid argument exit behavior;
- source bytes and metadata, scan history, assertions, retractions, and schema version remaining unchanged on success and failure.

Before completion run:

1. `uv run pytest`
2. `uv run ruff check .`
3. `uv run ruff format --check .`
4. `uv run mypy src tests`
5. `git diff --check`

## Non-goals

Exclude vocabulary mutation or administration, tag renaming, saved searches, content/file search changes, schema changes, JSON output, pagination, fuzzy matching, and archive operations.

## Exit condition

Record the implementation outcome and validation results, mark this subplan implemented, then mark umbrella Plan 003.3 complete. Stop before Plan 004.
